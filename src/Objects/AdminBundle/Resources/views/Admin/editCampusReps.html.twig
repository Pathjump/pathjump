{% extends '::base_admin.html.twig' %}
{% block title %}configurations{% endblock %}
{%block body%}
<h1>Edit Campus RepsText</h1>


<form  action="{{ url('admin_edit_CampusReps') }}" method="post" {{ form_enctype(form) }}>
    {{ form_errors(form) }}
        <table>
            
            <tr>
                <td>CampusReps Text</td>
                <td>{{ form_widget(form.CampusReps , {'attr': {'value': config.CampusReps} }) }}</td>
     
            </tr>
           
                <td>{{ form_rest(form) }}</td>
                <td><button type="submit">Submit</button></td>
                <td></td>
            </tr>
        </table>
        
         
    </form>
{% endblock%}

{% block javascripts %}
{#<script src="{{ asset('js/tiny_mce.js') }}"></script>#}
{#<script>
    
tinyMCE.init({
        mode : "textareas",
        theme : "advanced",
        // Theme options - button# indicated the row# only
        theme_advanced_buttons1 : "newdocument,|,bold,italic,underline,|,justifyleft,justifycenter,justifyright,fontselect,fontsizeselect,formatselect",
        theme_advanced_buttons2 : "cut,copy,paste,|,bullist,numlist,|,outdent,indent,|,undo,redo,|,link,unlink,anchor,image,|,code,preview,|,forecolor,backcolor",
        theme_advanced_buttons3 : "insertdate,inserttime,|,spellchecker,advhr,,removeformat,|,sub,sup,|,charmap,emotions",      
        theme_advanced_toolbar_location : "top",
        theme_advanced_toolbar_align : "left",
        theme_advanced_statusbar_location : "bottom",
        theme_advanced_resizing : true

    });    */
    </script>#}
    
    <script type="text/javascript" src="{{asset('js/ckeditor/ckeditor.js')}}"></script>
    <script type="text/javascript">
            window.onload = function()
            {
                    CKEDITOR.replace( 'form_CampusReps' );
            }
        </script>
    
{% endblock %}