{% extends '::base_admin.html.twig' %}
{% block title %}Edit employers data page{% endblock %}

{% block body %}
<h1>Edit employers data page</h1>

{% if message %}
<div class="alert-message{% if status == 'success' %} success{% else %} error{% endif %}">
    <p>{{ message }}</p>
</div>
{% endif %}

<form  action="{{ url('admin_edit_employers_data_page') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form.employersData) }}
    {{ form_rest(form) }}
        <button type="submit" id="about-submit" class="btn">Submit</button>
</form>
{% endblock %}
{% block javascripts %}
{{ parent() }}
{#
<script src="{{ asset('js/tiny_mce.js') }}"></script>
<script>
tinyMCE.init({
        mode : "textareas",
        theme : "advanced",
        width :"800",
        height : "600",
        // Theme options - button# indicated the row# only
        theme_advanced_buttons1 : "newdocument,|,bold,italic,underline,|,justifyleft,justifycenter,justifyright,fontselect,fontsizeselect,formatselect",
        theme_advanced_buttons2 : "cut,copy,paste,|,bullist,numlist,|,outdent,indent,|,undo,redo,|,link,unlink,anchor,image,|,code,preview,|,forecolor,backcolor",
        theme_advanced_buttons3 : "insertdate,inserttime,|,spellchecker,advhr,,removeformat,|,sub,sup,|,charmap,emotions",      
        theme_advanced_toolbar_location : "top",
        theme_advanced_toolbar_align : "left",
        theme_advanced_statusbar_location : "bottom",
        theme_advanced_resizing : true

    });    
    
     $(document).ready(function () {
            $('#about-submit').click(function(){
                
                    
                var article = tinyMCE.editors[0].getContent();
                if(($.trim(article)).length > 0){
                    $('#form_employersData').val(article);
                }
                
                return true;
            });
            });
    </script>
    
    #}
        <script type="text/javascript" src="{{asset('js/ckeditor/ckeditor.js')}}"></script>
    <script type="text/javascript">
            window.onload = function()
            {
                    CKEDITOR.replace( 'form_employersData' );
            }
        </script>
{% endblock %}