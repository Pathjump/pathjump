<ul class="manage-list pull-right">
    <li><a class="Notifi  active notifiToTooltip" data-original-title="Your Notifications" data-placement="bottom" data-toggle="tooltip" href="{{path('company_notification')}}"></a>
                {% if allCompanyNotificationsCount > 0 %}
            <span class="noti-num">{{allCompanyNotificationsCount}}</span>
                {% endif %}
        </li>
        <li><a class="Messag messageTooltip" data-original-title="Messages" data-placement="bottom" data-toggle="tooltip" href="{{path('company_box',{'box' : 'inbox'})}}"></a>
                {% if newMessagesCount > 0 %}
                <span class="noti-num">{{newMessagesCount}}</span>
                {% endif %}
            </li>
            <li><a class="Sitt editTooltip" data-original-title="Edit Profile" data-placement="bottom" data-toggle="tooltip" href="{{path('company_edit')}}"></a></li>
        </ul>

